name: Build
description: Run Build
runs:
  using: "composite"
  steps:
    - name: Install deps
      run: npm install --silent
      shell: bash
    - name : Run prepack
      run: npm run prepack
      shell: bash
