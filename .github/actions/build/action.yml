name: Build
description: Run Build
runs:
  using: "composite"
  steps:
    - name: Install deps
      run: npm install --silent
      shell: bash
    - name : Build and package the application
      run: npm run pack
      shell: bash
